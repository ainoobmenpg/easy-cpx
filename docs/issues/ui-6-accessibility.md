# UI-6: アクセシビリティ/ショートカット/フォーカス

### 現状
- ボタンに `aria-label` は部分的。キーボード操作の網羅/フォーカスリング改善が必要。

### 対応
- 主要操作のショートカット（Z:ズームリセット、L:凡例、H:ヘルプ、M:モード切替）。
- 重要ボタンへ `aria-pressed`/`aria-label` 追加、フォーカスリングの可視化。

### 完了条件
- キーボードのみで主要操作が可能で、色覚/コントラストも基準を満たす。
