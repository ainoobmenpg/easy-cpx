# UI-2: 存在しない `/new-game` リダイレクトの修正

### 症状
- `frontend/app/game/page.tsx:178,183` で `/new-game` へ `router.replace()` しているが、そのルートは存在せず、`/games` が正しい。

### 対応
- 両箇所を `/games` に置換。空のゲーム時もゲーム一覧/シナリオ選択へ誘導。

### 完了条件
- ゲーム未作成時に確実に遷移し、404が発生しない。
