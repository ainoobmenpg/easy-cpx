# CPX-SEC: レート制限＋スキーマ検証＋監査ログの強化

### 背景
生成系API/内部APIの悪用防止。全APIでスキーマ検証・レート制限・構造化監査ログを適用。

### 提案
- レート制限（IP/ユーザ単位）
- Pydantic/JSON Schema検証の徹底（Ajv等でFE側も）
- 監査ログ（user/game/endpoint/許可/拒否/所要時間）

### 受け入れ基準
- スパム的アクセスが抑止され、拒否がログに残る
- スキーマ不一致は 422 で弾く
