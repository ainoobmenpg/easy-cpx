# CPX-VALIDATION: 生成AI出力のスキーマ検証/サニタイズ

### 背景
`ai_client` の出力をそのまま使用しており、型/値の保証が弱い。

### 提案
- `shared/schemas/*.schema.json` に対するJSON Schema検証を導入（注文/レポート）。
- フェイルセーフ: 不正時はフォールバック/修復、監査ログに記録。

### 受け入れ基準
- 無効なAI出力がデータ層に到達しない（テストあり）。
